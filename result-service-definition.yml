 
apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result-service
  namespace: vote
spec:
  type: NodePort
  ports:
  - name: "result-service"
    port: 5000
    targetPort: 80
    nodePort: 31000
  selector:
    app: result